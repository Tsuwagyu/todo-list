export class Project {

    constructor(name) {
        this.name = name;
        this.todoHolder = [];
    }
}

const projectManager = {

    projectCollection = [],

    add(name) {
        new Project       
    }

    remove(project) {}

    get(project) {}
}